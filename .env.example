# ── Local development (docker compose or local Postgres) ─────────────────────
# Used by the Python backend if POSTGRES_URL_NON_POOLING is not set.
DATABASE_URL=postgresql://indirectrates:dev@localhost:5432/indirectrates


# ── Vercel + Neon (automatically injected by the Vercel Neon integration) ─────
# These are set automatically when you connect Neon via Vercel Storage.
# You do NOT need to set these manually on Vercel.
#
# POSTGRES_URL=              ← pooled (pgbouncer) — used by Better Auth / pg
# POSTGRES_URL_NON_POOLING=  ← direct connection  — used by FastAPI / psycopg2
# POSTGRES_USER=
# POSTGRES_PASSWORD=
# POSTGRES_HOST=
# POSTGRES_DATABASE=


# ── Next.js frontend  (Vercel env vars or web/.env.local for local dev) ───────

# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=change-me-generate-with-openssl-rand-base64-32

# Set to your Vercel deployment URL in production, localhost:3000 in dev
BETTER_AUTH_URL=http://localhost:3000
NEXT_PUBLIC_BETTER_AUTH_URL=http://localhost:3000

# Python FastAPI backend URL (Railway / Render / Fly.io in production)
BACKEND_URL=http://127.0.0.1:8000

# Optional: Social OAuth providers (leave blank to disable)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=


# ── Python FastAPI backend (Railway / Render / Fly.io env vars) ───────────────

# Neon direct connection string (from Vercel Neon integration → Connection details)
POSTGRES_URL_NON_POOLING=postgresql://user:password@ep-xxx.region.aws.neon.tech/dbname?sslmode=require

# Comma-separated list of allowed CORS origins
ALLOWED_ORIGINS=https://your-app.vercel.app,http://localhost:3000

# Optional: Bearer token for direct API access (Swagger UI / scripts / integrations).
# Set a strong random value. When omitted, Bearer auth is disabled entirely.
# Generate with: openssl rand -hex 32
API_KEY=
# The user ID to associate with requests authenticated via API_KEY.
# Use your actual Better Auth user UUID here for data to appear in the right account.
API_KEY_USER_ID=dev-user
