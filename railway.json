{
  "$schema": "https://railway.app/railway-schema.json",
  "build": {
    "buildCommand": "pip install -e '.[server,ai]'"
  },
  "deploy": {
    "startCommand": "python -m uvicorn indirectrates.server:app --host 0.0.0.0 --port $PORT"
  }
}
