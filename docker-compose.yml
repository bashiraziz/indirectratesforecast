services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: indirectrates
      POSTGRES_USER: indirectrates
      POSTGRES_PASSWORD: dev
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U indirectrates"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
